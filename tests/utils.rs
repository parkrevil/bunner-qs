//! Auto-generated skeleton from qs/test/utils.js
#![allow(unused)]

// original: merge()
mod merge {
}

// original: assign()
mod assign {
}

// original: combine()
mod combine {
    // original: both arrays
    #[test]
    fn both_arrays() {
    }

    // original: one array, one non-array
    #[test]
    fn one_array_one_non_array() {
    }

    // original: neither is an array
    #[test]
    fn neither_is_an_array() {
    }
}

// original: decode
mod decode {
}

// original: encode
mod encode {
    // original: symbols
    #[test]
    fn symbols() {
    }
}

// original: isBuffer()
mod isbuffer {
}

// original: isRegExp()
mod isregexp {
}
